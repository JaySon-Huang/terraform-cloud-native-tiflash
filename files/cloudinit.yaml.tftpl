#cloud-config
groups:
  - tidb: [ tidb ]
users:
  - default
  - name: ${username}
    groups: sudo
    shell: /bin/bash
    sudo: 'ALL=(ALL) NOPASSWD:ALL'
    ssh_authorized_keys:
      - ${ssh_public_key}
